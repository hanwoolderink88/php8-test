version: "3.9"
services:
  webserver:
    image: nginx:latest
    ports:
    - "8080:80"
    volumes:
    - ./nginx.conf:/etc/nginx/nginx.conf:ro
   php:
     build:
       dockerfile: Dockerfile
       context: .
